# Hermes CGM Agent — 个人连续血糖监测AI陪伴助手

**项目一句话介绍**：独立设计并开发了一款面向糖尿病患者的个人AI助手，对接连续血糖监测(CGM)硬件数据，帮助用户理解血糖规律、生成就诊报告，同时以"知情陪伴者"而非"医疗监督者"的定位与用户交互，全程在本地运行保护健康隐私。

---

## 产品经理视角（面向业务/HR）

1. **重新定义医疗AI交互范式**：针对传统医疗AI"命令式、评判式"交互引发患者抵触的问题，设计了"知情陪伴者"产品定位——不做诊断、不给指令、不评判用户行为，只基于用户自身历史数据提供参考信息，将"用户遇到问题愿意主动告诉AI"作为核心成功指标。这套交互规则（共6大原则、30+条话术规范）落地后，从产品机制上避免了AI引发用户焦虑或误导决策的风险。

2. **设计一套系统适配三类使用场景**：针对糖尿病管理中"用户日常看、医生就诊用、家属放心"三种完全不同的信息需求，在同一套数据基础上实现了三种叙事版本：用户版用生活化语言（30-80字，像微信聊天）、医生版用临床精确数字和结构化指标、家属版只发一句话平安播报。解决了"同一份报告给谁看都不合适"的行业常见问题。

3. **建立分级安全响应与情感关怀机制**：针对血糖异常的不同风险等级，设计了红/黄/绿三区响应机制——危急值（血糖过低/过高）直接触发硬安全拦截不经过AI生成；针对连续多天血糖波动的情况，设计了渐进式关心升级：第1天正常归因数据→第3天关心用户状态→第7天建议就医咨询，针对孕妇/老人/儿童等脆弱人群将升级阈值压缩一半；平稳期默认不推送消息，"沉默也是关怀"。

4. **打通血糖管理全链路产品闭环**：独立完成从0到1的完整产品功能设计：支持4种以上CGM数据源接入（Dexcom、微泰AiDEX、xDrip等），自动计算TIR（目标范围内时间）、GMI（糖化血红蛋白估算）等8项核心血糖指标，自动识别高低血糖事件，通过对话式交互生成日报/周报/就诊报告，支持Webhook/系统推送主动提醒，覆盖了糖尿病患者日常记录、周期复盘、就诊准备、家属告知全场景需求。

---

## AI工程师视角（面向技术面试官）

1. **设计医疗级安全的RAG与记忆系统架构**：针对医疗AI"不能编造医学建议"的核心要求，创新设计了双轨记忆隔离架构——医学知识库（580张中英双语临床论断卡，来自13篇官方指南）设为只读、零容错，用户个人记忆可写、可演化、可遗忘，两套系统物理隔离、单向写保护，冲突时医学知识优先；配合三层记忆分层（热数据SQL直取、温数据后台摘要合成、冷数据按需检索）和L0渐进式上下文压缩，既保证了医疗建议的权威性，又实现了个性化记忆。RAG检索准确率hit@3达到100%（73个评测用例）。

2. **将医疗安全护栏下沉到代码层（不依赖Prompt）**：为避免Prompt注入或幻觉导致的医疗风险，将所有安全规则从Prompt层下沉到代码执行层硬实现：①三区安全路由器——危急值直接截断所有LLM输出，返回预设安全提示，并设置2小时恢复复查窗口；②引用校验守卫——报告中出现的所有医疗数字必须有知识库原文引用和页码支撑，校验不通过直接阻断交付返回"无法确认"；③个人健康信息(PHI)字段级Fernet加密，密钥文件通过系统ACL控制权限（Windows下icacls、Linux下chmod 0o600）。从架构层面杜绝AI给出危险医疗建议的可能。

3. **搭建完整的工程化质量保障体系**：作为高风险医疗类项目，建立了严格的工程规范：①全程采用Spec-Driven Development流程，制定7条不可逾越的项目宪法，所有架构决策通过ADR（3份）和决策日志（50条D010-D059）记录，禁止无文档的"幽灵代码"；②编写**579个单元/集成测试**，覆盖80+测试文件，非LLM依赖测试100%通过；③配置GitHub Actions CI流水线，支持Ubuntu/Windows双操作系统×Python 3.11/3.12双版本矩阵测试，知识库变更设置hit@3≥0.95的硬性质量门禁；④实现14天确定性仿真回放管线，支持注入虚拟时钟和数据源，全链路可审计、可复现。

4. **实现与Hermes Agent的插件化无缝集成**：采用分层架构（领域模型→存储→服务→工具→入口）+仓储模式+声明式工具注册，通过薄适配器模式实现与Hermes Agent框架的共生集成——对外暴露19个工具（分9组，带JSON Schema契约和风险分级），实现MemoryProvider插件支持对话上下文自动注入记忆，**零修改**宿主Hermes框架代码，不自建独立聊天引擎，保持了架构的可替换性和可扩展性。

5. **构建自动化医学知识库生产管线**：实现了从临床指南PDF到可检索论断卡的完整自动化流程：PDF版面感知解析→调用多模态大模型抽取结构化论断→质量过滤去重→人工审核签纳入库。当前已从ADA、AACE等13篇权威临床指南中自动抽取并初步校验了580张中英双语论断卡，覆盖血糖目标、低血糖处理、特殊人群等核心主题，为RAG系统提供了高质量的权威知识基础。
