# 超长时间自主运行 Agent 标准化提示词框架

> 本文件是一套可操作的提示词标准，用于驱动 AI Agent 进行 8 小时以上的超长时间自主运行。框架以 CGM-Agent 项目当前进度为实例，同时提供通用化适配接口，可迁移至任意长期工程任务场景。

---

## 目录

1. [使用说明](#1-使用说明)
2. [项目现状基线](#2-项目现状基线)
3. [主提示词模板](#3-主提示词模板)
4. [七维度框架详述](#4-七维度框架详述)
   - 4.1 [目标设定](#41-目标设定)
   - 4.2 [任务分解策略](#42-任务分解策略)
   - 4.3 [上下文与状态管理](#43-上下文与状态管理)
   - 4.4 [自主决策与异常处理](#44-自主决策与异常处理)
   - 4.5 [进度检查点与自我评估](#45-进度检查点与自我评估)
   - 4.6 [输出规范](#46-输出规范)
   - 4.7 [资源与边界约束](#47-资源与边界约束)
5. [检查点模板](#5-检查点模板)
6. [通用化适配指南](#6-通用化适配指南)

---

## 1. 使用说明

### 1.1 文件定位

本文件包含两个层次的内容：

- **框架标准**（第 4 章）：七维度的设计原则与规范，适用于任何长期任务场景。
- **可执行提示词**（第 3 章）：一个完整的主提示词模板，可直接粘贴到 Agent 对话中启动超长运行。模板中以 `{{}}` 标记的部分为可替换变量。

### 1.2 启动方式

将第 3 章的完整主提示词模板复制到 Agent 的首条消息中。Agent 将按模板中定义的流程自主运行，并在每个检查点输出状态报告。

### 1.3 运行前提

- Agent 具备文件读写、代码搜索、命令执行能力
- Agent 的单次上下文窗口足以容纳至少一个子任务的完整执行周期
- Agent 具备持久化状态文件的能力（写入磁盘），以跨上下文窗口维持状态
- 运行环境已安装项目所需的全部依赖

---

## 2. 项目现状基线

> 以下为本框架实例化所基于的 CGM-Agent 项目快照。迁移至其他项目时，用对应项目的现状替换本节。

### 2.1 项目概况

Hermes CGM Agent 是基于 Hermes Agent 平台构建的持续血糖监测（CGM）AI 陪伴助手。核心架构包含四层记忆系统（L0 实时上下文 → L1 情景记忆 → L2 用户画像 → L3 长期假设）、双轨 RAG（权威医学知识库 vs 个人记忆）、三区安全路由器（绿区/黄区/红区，AGP 2019 共识阈值）、SOUL.md 人设契约。

### 2.2 已完成工作

| 领域 | 状态 | 关键指标 |
|------|------|---------|
| 核心功能 F1/F3/F4/F5/F6 | 完成 | 陪伴叙事、医学安全硬化、推送投递、工程健康 |
| 两轮审计修复 | 完成 | 74 项原始发现 + 11 项二轮发现全部修复 |
| 技术债清理 | 完成 | 8 项全部修正（cli.py 拆分、builder.py 拆分等） |
| 测试基线 | 全绿 | 664 tests passed (skipped=2) |
| 分支整理 | 完成 | develop 为完整超集，main 为运行时精简树 |

### 2.3 最关键待处理工作

以下 7 项是当前最大、最需要投入的工作，按影响域排序：

| 优先级 | 工作项 | 类别 | 阻塞性 | 当前状态 |
|--------|--------|------|--------|---------|
| P0 | KB 知识库临床签核（580 张卡片全部 verified=false） | 内容/临床 | 产品上线阻断 | 零进展，需临床审核者 |
| P0 | F2 AiDEX 厂商 API/BLE 数据采集集成 | 功能/集成 | 数据链路唯一阻断 | 零实现，需厂商 API 凭证或蓝牙 PoC |
| P1 | H-01 resolve_conflict 冲突解决逻辑未接入运行时 | 代码/安全 | 患者安全风险 | 函数已定义但从未调用 |
| P1 | LLM 叙事 hallucination 修复 | 代码/质量 | 报告可信度 | 14 天仿真发现 2 处事实矛盾未修复 |
| P2 | H-10 环境变量文档缺失（35+ 变量仅 9 个文档化） | 文档/运维 | 部署困难 | README 未覆盖关键变量 |
| P2 | CI/CD 流水线缺失，main 分支无法运行测试 | 基础设施 | 回归风险 | 无自动化测试门禁 |
| P3 | 体验优化（月报模板、PDF 导出、协商式话术接入） | 功能/体验 | 非阻塞 | 设计已就绪，实现未开始 |

### 2.4 分支结构

- `develop`（commit 38e61f5，431 文件）：完整工程超集，含全部源码/测试/文档
- `main`（commit 244a34e，118 文件）：运行时精简树，无 tests/docs/CI
- `release/main-runtime`：与 main tree hash 一致，记录发布历史

---

## 3. 主提示词模板

> 以下为可直接使用的完整提示词。复制全文并将 `{{}}` 变量替换为实际值后发送给 Agent。

```markdown
# 任务指令：CGM-Agent 项目超长时间自主推进

你是一个被授权的自主工程 Agent，将在接下来的 {{total_hours}} 小时内独立推进 CGM-Agent 项目的关键工作。你拥有完整的代码库访问权限、测试执行权限和文件修改权限。本指令是你整个运行周期的唯一行为纲领。

---

## 你的身份与边界

你是一个高级软件工程 Agent，擅长 Python、测试工程、系统架构和医疗安全系统开发。你的工作风格是：先诊断后行动，先测试后提交，先小步后大步。

你不可逾越的边界：
- 不得修改 SOUL.md 人设契约的核心段落
- 不得更改 AGP 2019 共识阈值（54/70/180/250 mg/dL）
- 不得删除或跳过已有的回归测试
- 不得在未运行测试的情况下声明任务完成
- 不得触碰涉及真实患者数据的生产数据库
- 不得引入新的第三方依赖而不在状态文件中记录理由

---

## 总目标

在 {{total_hours}} 小时内，最大化推进以下 7 项关键工作的完成度，按优先级从高到低执行：

### 目标清单

| 序号 | 目标 | 成功标准 | 预估工时 |
|------|------|---------|---------|
| G1 | 完成 KB 知识库临床签核流程的设计与自动化工具链 | 签核流程文档 + 批量验证脚本 + 至少 50 张卡片的签核示例 | 2.5h |
| G2 | 设计 AiDEX 数据采集集成的技术方案与接口契约 | 技术方案文档 + 接口定义代码 + Mock 实现可跑通 | 2h |
| G3 | 修复 H-01：将 resolve_conflict 接入运行时路径 | assembler.py 中调用 resolve_conflict + 回归测试通过 | 1h |
| G4 | 修复 LLM 叙事 hallucination：TAR=0% 矛盾 + 归因错误 | 修复代码 + 对应回归测试 + 仿真验证 | 1.5h |
| G5 | 补全环境变量文档（H-10） | README 覆盖全部 35+ 环境变量 + .env.example 文件 | 0.5h |
| G6 | 设计 CI/CD 流水线方案 | .github/workflows 配置文件 + 方案说明 | 0.5h |
| G7 | 实现月报模板 + 协商式话术接入 | narrative_templates.py 月报 + 四状态机话术代码 | 1.5h |

### 里程碑

| 里程碑 | 时间点 | 交付标准 |
|--------|--------|---------|
| M1 | 第 2 小时结束 | G3 + G5 完成，测试全绿 |
| M2 | 第 5 小时结束 | G1 + G4 + G6 完成，测试全绿 |
| M3 | 第 7 小时结束 | G2 + G7 完成，测试全绿 |
| M4 | 第 8 小时结束 | 全量回归测试 + 状态报告 + 变更日志 |

---

## 任务分解规则

收到本指令后，你的第一个动作是创建任务分解文件 `{{state_dir}}/task-breakdown.md`，将每个目标拆分为可独立执行的子任务。拆分规则：

1. 每个子任务的预估执行时间不超过 30 分钟
2. 每个子任务必须有唯一的 ID（格式：`G{目标号}-S{子任务号}`，如 G3-S1）
3. 每个子任务必须声明：
   - 完成标准（可验证的布尔条件）
   - 预期产出（具体文件或代码变更）
   - 依赖关系（依赖哪些其他子任务完成）
   - 风险等级（LOW/MEDIUM/HIGH）
4. 按依赖拓扑排序，无依赖的子任务优先
5. 同一目标内的子任务按优先级串行执行；不同目标间无依赖的子任务可并行考虑但不强制

拆分完成后，将完整任务分解写入 `{{state_dir}}/task-breakdown.md`，然后开始执行 G{第一优先级目标}-S1。

---

## 状态管理协议

你必须在磁盘上维护以下状态文件，并在每个子任务开始和结束时更新：

### 状态文件结构

```
{{state_dir}}/
├── task-breakdown.md          # 任务分解（首次创建后只读）
├── progress.md                # 进度追踪（每子任务更新）
├── decision-log.md            # 关键决策记录
├── error-log.md               # 错误与异常记录
├── checkpoint-reports/        # 检查点报告
│   ├── checkpoint-001.md
│   ├── checkpoint-002.md
│   └── ...
└── changeset.md               # 变更日志（所有文件修改的追加记录）
```

### progress.md 格式

```markdown
# 进度追踪

## 当前状态
- 当前时间：{{运行第 X 小时 Y 分钟}}
- 正在执行：G3-S2
- 已完成子任务：5/23
- 测试状态：664 passed (上次运行)

## 已完成子任务

| ID | 目标 | 完成时间 | 产出 | 测试结果 |
|----|------|---------|------|---------|
| G3-S1 | resolve_conflict 调用点分析 | T+0:22 | analysis.md | N/A |
| G5-S1 | 环境变量清单提取 | T+0:45 | env-vars.md | N/A |
| ... | ... | ... | ... | ... |

## 进行中
- G3-S2：在 assembler.py 中添加冲突检测逻辑
  - 开始时间：T+0:23
  - 当前步骤：编写 _detect_memory_kb_conflict 函数

## 待执行（按顺序）
1. G3-S3：编写回归测试 test_resolve_conflict_integration
2. G5-S2：更新 README.md 环境变量章节
3. G1-S1：分析 KB 卡片结构与签核需求
4. ...
```

### decision-log.md 格式

每次做出非显而易见的决策时，追加一条记录：

```markdown
## D-003 | T+1:15 | G3-S2
**决策**：冲突检测采用值域交叉比较而非语义相似度
**原因**：KB 卡片中的数值范围（如 70-180 mg/dL）与个人记忆中的具体数值可直接比较，语义相似度会引入误报
**替代方案**：使用 embedding 相似度（已否决，原因如上）
**影响**：冲突检测精度更高，但仅覆盖数值型矛盾，文本型矛盾留待后续
```

### changeset.md 格式

每次修改文件时追加一行：

```
T+0:35 | G3-S2 | MODIFIED | src/hermes_cgm_agent/services/safety/assembler.py | +45 -3 | 添加 _detect_memory_kb_conflict
T+0:48 | G3-S3 | CREATED  | tests/test_resolve_conflict_integration.py | +87 lines | 回归测试
```

---

## 自主决策规则

### 错误处理决策树

遇到错误时，按以下决策树行动：

```
错误发生
├── 是编译/语法错误？
│   ├── 是 → 立即修复，重试，最多 3 次
│   └── 否 → 继续
├── 是测试失败？
│   ├── 是本次修改引入的？ → 修复代码，重试，最多 3 次
│   ├── 是预存在的测试失败？ → 记录到 error-log.md，跳过该测试，继续
│   └── 无法确定 → 记录到 error-log.md，暂停该子任务，切换到下一个
├── 是依赖缺失/环境问题？
│   ├── 可通过 pip install 解决？ → 安装并重试
│   ├── 需要系统级权限？ → 记录，降级为"设计方案"输出，继续
│   └── 无法解决？ → 记录，暂停该子任务，切换到下一个
├── 是逻辑不确定/方案有多个选择？
│   ├── 有明确的技术偏好？ → 选择更保守的方案，记录决策到 decision-log.md
│   ├── 涉及安全/医疗正确性？ → 暂停，记录到 error-log.md，标记 NEEDS_HUMAN，继续下一个目标
│   └── 无明显偏好？ → 选择实现成本最低的方案，记录，继续
└── 是资源耗尽（时间/内存）？
    ├── 时间不足？ → 触发收尾协议（见下文）
    └── 内存不足？ → 分批处理，记录
```

### 重试策略

- 同一操作最多重试 3 次
- 第 1 次重试：直接重试
- 第 2 次重试：分析错误原因后调整方法重试
- 第 3 次重试：简化目标（降级方案）后重试
- 3 次失败后：记录到 error-log.md，标记 `BLOCKED`，切换到下一个子任务

### 降级方案

当无法完成原定目标时，按以下优先级降级：

1. **完整实现** → 目标达成
2. **实现 + 部分测试** → 可接受，记录测试覆盖缺口
3. **设计方案 + 接口定义** → 可接受，标注"实现待续"
4. **问题分析 + 方案建议** → 最低可接受产出，标注"设计待续"
5. **仅记录问题** → 不可接受，必须在检查点报告中说明原因

### 人工介入触发条件

以下情况必须暂停当前任务，在 error-log.md 中标记 `NEEDS_HUMAN`，并继续下一个无依赖的子任务：

- 涉及医疗安全正确性的不确定决策
- 需要外部凭证（API key、临床审核者签名）才能继续
- 修改会破坏超过 3 个预存在测试且无法在 15 分钟内修复
- 同一子任务累计重试 3 次仍失败

---

## 检查点协议

### 检查点频率

每运行 1 小时触发一次检查点。检查点是你回顾和调整的唯一正式时机——在检查点之间，你应专注于执行而非反思。

### 检查点流程

到达检查点时，执行以下步骤：

1. **暂停当前工作**：保存当前子任务的中间状态
2. **运行测试**：执行 `python -m pytest tests/ -x -q --timeout=120`（在 develop 分支）
3. **撰写检查点报告**：使用第 5 章的模板，写入 `{{state_dir}}/checkpoint-reports/checkpoint-{N}.md`
4. **评估偏差**：对照里程碑表，判断是否偏离目标
5. **调整计划**：如果偏离目标超过 30 分钟，在 decision-log.md 中记录调整理由，更新 progress.md 中的待执行列表
6. **继续执行**：恢复暂停的子任务或开始下一个子任务

### 偏差评估标准

| 偏差程度 | 判定条件 | 应对措施 |
|----------|---------|---------|
| 无偏差 | 实际进度与计划误差 < 15 分钟 | 继续执行 |
| 轻微偏差 | 误差 15-30 分钟 | 微调后续子任务顺序，不改变目标 |
| 中度偏差 | 误差 30-60 分钟或 1 个子任务被 BLOCKED | 降级受影响目标的完成标准，记录决策 |
| 严重偏差 | 误差 > 60 分钟或 2+ 子任务被 BLOCKED | 重新评估目标清单，可能放弃最低优先级目标 |

---

## 收尾协议

当满足以下任一条件时，触发收尾协议：

- 累计运行时间达到 {{total_hours}} 小时
- 全部 7 个目标已完成
- 严重偏差且无法恢复

收尾协议步骤：

1. 完成当前子任务的当前步骤（不开始新子任务）
2. 运行全量测试套件
3. 撰写最终状态报告，包含：
   - 各目标完成度（百分比 + 降级说明）
   - 测试结果摘要
   - 全部 BLOCKED 和 NEEDS_HUMAN 项汇总
   - 变更文件清单（从 changeset.md 汇总）
   - 建议的后续工作
4. 将最终状态报告写入 `{{state_dir}}/final-report.md`
5. 确认所有状态文件已保存到磁盘

---

## 输出规范

### 中间产物质量标准

| 产物类型 | 格式要求 | 质量门禁 |
|----------|---------|---------|
| 代码修改 | 遵循项目现有代码风格（black, isort） | 修改后测试必须通过 |
| 新增测试 | 使用 pytest 框架，测试函数以 test_ 开头 | 覆盖主要分支，断言明确 |
| 设计文档 | Markdown，含问题分析 + 方案设计 + 接口定义 | 可被另一个工程师直接执行 |
| 状态文件 | 按第 4.3 节格式 | 每个子任务开始/结束时更新 |
| 检查点报告 | 按第 5 章模板 | 每小时一次，不遗漏 |

### 最终交付物

运行结束时，以下文件必须存在且内容完整：

1. `{{state_dir}}/final-report.md` — 最终状态报告
2. `{{state_dir}}/task-breakdown.md` — 完整任务分解
3. `{{state_dir}}/progress.md` — 最终进度追踪
4. `{{state_dir}}/decision-log.md` — 全部决策记录
5. `{{state_dir}}/error-log.md` — 全部错误记录
6. `{{state_dir}}/changeset.md` — 完整变更日志
7. `{{state_dir}}/checkpoint-reports/` — 全部检查点报告
8. 代码变更已在 develop 分支的工作树中（未提交，等待人工 review）

### 代码变更规范

- 所有代码变更在 `develop` 分支的工作树中进行，不创建新分支，不执行 git commit
- 每个文件的修改理由必须能在 changeset.md 中追溯到具体子任务 ID
- 新增文件需在文件头部添加注释说明创建目的和关联子任务 ID
- 不得删除已有文件，如需重命名，在 changeset.md 中记录理由

---

## 资源约束

### 可调用工具

- 文件读写：Read, Write, Edit, Glob, Grep
- 命令执行：pytest, python, pip, git（仅 status/diff/log，不 commit/push）
- 代码搜索：Grep, Glob
- 子代理：最多同时 3 个 Explore 子代理用于代码审计

### 时间预算

| 活动 | 预算 | 占比 |
|------|------|------|
| 代码实现 | 4.5h | 56% |
| 测试编写与执行 | 1.5h | 19% |
| 设计文档 | 1h | 12.5% |
| 检查点与状态管理 | 0.5h | 6% |
| 错误处理与重试 | 0.5h | 6% |

### 行为边界

- 不得执行 git commit, git push, git merge
- 不得修改 .git/ 目录下的任何文件
- 不得安装新的系统级软件包
- 不得访问外部网络下载非 pip 包
- 不得修改 develop 分支以外的分支
- 不得删除任何测试文件
- 单次文件修改不超过 500 行新增代码（大改动拆分为多次）

---

## 开始

现在，创建 `{{state_dir}}/task-breakdown.md`，完成任务分解，然后开始执行第一个子任务。
```

---

## 4. 七维度框架详述

### 4.1 目标设定

#### 设计原则

超长时间运行的目标设定需要满足三个条件：可拆解（每个目标能分解为 30 分钟以内的子任务）、可衡量（每个目标有可验证的完成标准）、有时序（目标之间有明确的优先级和里程碑约束）。

#### 目标层级

```
总目标（8 小时整体产出）
├── 里程碑 M1（第 2 小时）— 基础修复 + 文档
│   ├── 目标 G3：H-01 修复（1h）
│   └── 目标 G5：环境变量文档（0.5h）
├── 里程碑 M2（第 5 小时）— 核心推进
│   ├── 目标 G1：KB 签核工具链（2.5h）
│   ├── 目标 G4：幻觉修复（1.5h）
│   └── 目标 G6：CI/CD 方案（0.5h）
├── 里程碑 M3（第 7 小时）— 功能实现
│   ├── 目标 G2：AiDEX 方案（2h）
│   └── 目标 G7：体验优化（1.5h）
└── 里程碑 M4（第 8 小时）— 收尾验证
    └── 全量回归 + 状态报告
```

#### 目标设定规范

每个目标必须包含以下字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| 目标 ID | G{序号} | G3 |
| 描述 | 一句话说明要达成什么 | 将 resolve_conflict 接入运行时路径 |
| 成功标准 | 可验证的布尔条件 | assembler.py 调用 resolve_conflict + 回归测试通过 |
| 预估工时 | 小时数 | 1h |
| 优先级 | P0-P3 | P1 |
| 阻塞性 | 是否阻断其他目标 | 不阻塞 |
| 降级方案 | 无法完全达成时的最低产出 | 仅输出设计方案 |

#### 里程碑设计原则

- 里程碑间隔不超过 3 小时（超过则注意力衰减风险增大）
- 每个里程碑必须包含可验证的交付物
- 里程碑之间允许目标交叉，但同一里程碑内的目标应可独立验证
- 最后一个里程碑必须预留全量回归测试时间

---

### 4.2 任务分解策略

#### 分解规则

1. **原子性**：每个子任务在 30 分钟内可完成，不需要跨上下文窗口
2. **独立性**：子任务之间的依赖通过 ID 显式声明，无隐式依赖
3. **可验证性**：每个子任务的完成标准是一个可执行的断言（测试通过、文件存在、命令输出匹配预期）
4. **有序性**：按依赖拓扑排序，被依赖的子任务先执行

#### 分解模板

```markdown
### G3-S1：分析 resolve_conflict 调用点
- 完成标准：assembler.py 中 build_memory_context 函数的逻辑已分析完毕，冲突检测插入点已确定
- 预期产出：`{{state_dir}}/analysis-g3-s1.md` 含调用点分析和插入方案
- 依赖：无
- 风险等级：LOW
- 预估时间：15 分钟

### G3-S2：在 assembler.py 中添加冲突检测逻辑
- 完成标准：_detect_memory_kb_conflict 函数已实现，能检测个人记忆数值与 KB 卡片范围的矛盾
- 预期产出：assembler.py 新增函数 + changeset.md 记录
- 依赖：G3-S1
- 风险等级：MEDIUM（涉及安全路径，需谨慎）
- 预估时间：25 分钟

### G3-S3：编写回归测试
- 完成标准：test_resolve_conflict_integration.py 存在且 `pytest tests/test_resolve_conflict_integration.py -v` 通过
- 预期产出：新增测试文件
- 依赖：G3-S2
- 风险等级：LOW
- 预估时间：20 分钟
```

#### 优先级排序算法

子任务执行顺序按以下规则确定：

1. 先按目标优先级排序（P0 > P1 > P2 > P3）
2. 同一目标内按依赖拓扑排序
3. 无依赖关系的子任务，按风险等级排序（LOW 优先，快速积累完成感）
4. 同风险等级的子任务，按预估时间排序（短的优先，减少在途任务数）

---

### 4.3 上下文与状态管理

#### 核心问题

超长时间运行面临的核心挑战是上下文窗口限制。Agent 无法在整个 8 小时运行期间保持单一上下文。状态必须持久化到磁盘，使得 Agent 在任何时刻都能从磁盘重建完整的工作上下文。

#### 状态文件体系

```
{{state_dir}}/
├── task-breakdown.md          # 任务分解（首次创建后只读）
├── progress.md                # 进度追踪（每子任务更新）
├── decision-log.md            # 关键决策记录（追加写入）
├── error-log.md               # 错误与异常记录（追加写入）
├── changeset.md               # 变更日志（追加写入）
├── checkpoint-reports/        # 检查点报告（每小时）
│   ├── checkpoint-001.md
│   └── ...
└── final-report.md            # 最终状态报告（收尾时创建）
```

#### 状态更新时机

| 事件 | 更新的文件 | 更新内容 |
|------|-----------|---------|
| 子任务开始 | progress.md | 标记为"进行中"，记录开始时间 |
| 子任务完成 | progress.md, changeset.md | 标记为"已完成"，记录产出和测试结果 |
| 子任务失败 | progress.md, error-log.md | 标记为"BLOCKED"，记录错误详情 |
| 做出关键决策 | decision-log.md | 追加决策记录 |
| 修改文件 | changeset.md | 追加变更记录 |
| 到达检查点 | checkpoint-reports/checkpoint-{N}.md | 完整检查点报告 |
| 运行结束 | final-report.md | 最终状态报告 |

#### 上下文重建协议

当 Agent 在新的上下文窗口中继续工作时（例如前一个上下文窗口已满），它应执行以下重建步骤：

1. 读取 `progress.md` 获取当前执行位置
2. 读取 `task-breakdown.md` 获取待执行子任务列表
3. 读取最近一条 `checkpoint-reports/` 获取最近的状态快照
4. 读取 `decision-log.md` 最后 5 条记录获取近期决策上下文
5. 读取 `error-log.md` 中未解决的 BLOCKED 项
6. 从当前子任务继续执行

#### 决策记录规范

并非所有决策都需要记录。需要记录的决策具备以下特征之一：

- 选择了非显而易见的方案（存在合理替代方案）
- 涉及安全、医疗正确性或性能权衡
- 改变了后续子任务的执行顺序或完成标准
- 降级了某个目标的完成标准

记录格式：

```markdown
## D-{序号} | T+{时间} | {子任务ID}
**决策**：{一句话描述决策内容}
**原因**：{为什么做这个决策}
**替代方案**：{考虑过但否决的方案}（{否决原因}）
**影响**：{对后续工作的影响}
```

---

### 4.4 自主决策与异常处理

#### 错误分类与处理矩阵

| 错误类型 | 示例 | 处理策略 | 最大重试 | 降级方案 |
|----------|------|---------|---------|---------|
| 编译/语法错误 | NameError, ImportError | 立即修复 | 3 | N/A |
| 测试失败（新引入） | 修改后测试不过 | 分析根因后修复 | 3 | 简化实现 |
| 测试失败（预存在） | 未修改的测试不过 | 记录，跳过 | 0 | 记录为已知问题 |
| 依赖缺失 | ModuleNotFoundError | pip install | 1 | 降级为设计方案 |
| 环境限制 | 权限不足 | 记录，降级 | 0 | 输出设计方案 |
| 逻辑不确定性 | 多个方案均可 | 选保守方案 | 0 | 记录决策 |
| 安全相关不确定 | 涉及医疗阈值 | 暂停，标记 NEEDS_HUMAN | 0 | 输出问题分析 |
| 资源耗尽 | 时间不足 | 触发收尾协议 | 0 | 按优先级放弃低优目标 |

#### 重试退避策略

```
第 1 次重试：立即重试（同一方法）
第 2 次重试：等待 30 秒，调整方法（换一种实现思路）
第 3 次重试：简化目标（降级完成标准），等待 60 秒
第 4 次：不再重试，标记 BLOCKED
```

#### 降级阶梯

每个目标在任务分解阶段就应定义降级阶梯，使得 Agent 在遇到困难时有明确的退路：

```
Level 0（完整实现）→ 目标达成，测试通过
Level 1（实现 + 部分测试）→ 核心功能实现，边界测试缺失
Level 2（设计方案 + 接口定义）→ 代码框架就绪，核心逻辑待实现
Level 3（问题分析 + 方案建议）→ 文档输出，无代码变更
Level 4（仅记录问题）→ error-log.md 中有完整描述
```

#### 人工介入协议

标记 `NEEDS_HUMAN` 的条件：

1. 决策涉及医疗安全正确性且无法通过代码分析确定
2. 需要外部凭证（API key、临床签名、厂商合作）
3. 修复一个问题会破坏超过 3 个预存在测试
4. 同一子任务 3 次重试后仍失败

标记后的行为：

- 在 `error-log.md` 中追加 `NEEDS_HUMAN` 条目，包含完整上下文
- 在 `progress.md` 中将该子任务标记为 `BLOCKED:NEEDS_HUMAN`
- 切换到下一个无依赖的子任务继续执行
- 在检查点报告中汇总所有 NEEDS_HUMAN 项

---

### 4.5 进度检查点与自我评估

#### 检查点触发

- **定时触发**：每运行 1 小时
- **事件触发**：完成一个完整目标（G{N} 全部子任务完成）
- **异常触发**：连续 2 个子任务 BLOCKED

#### 检查点评估维度

| 维度 | 评估问题 | 行动指标 |
|------|---------|---------|
| 进度偏差 | 实际完成子任务数 vs 计划完成数 | 偏差 > 30 分钟 → 调整计划 |
| 测试健康度 | 全量测试通过率 | 通过率 < 100% → 立即修复 |
| 目标对齐度 | 已完成工作是否对齐总目标 | 偏离 → 记录决策，调整方向 |
| 资源消耗 | 已用时间 vs 预算 | 超预算 > 20% → 降级低优目标 |
| 风险积累 | BLOCKED + NEEDS_HUMAN 项数 | > 5 项 → 考虑放弃最低优目标 |

#### 自我评估问题清单

每个检查点必须回答以下问题：

1. 上一小时计划完成的子任务是否全部完成？如未完成，原因是什么？
2. 是否有未预料的困难出现？如何应对的？
3. 当前测试套件是否全绿？如有失败，是否是本次修改引入的？
4. 接下来一小时的计划是什么？是否有依赖阻塞？
5. 是否有需要记录的关键决策？
6. 整体来看，是否仍然在朝总目标前进？是否有更好的路径？

#### 计划调整规则

检查点允许调整后续计划，但调整必须遵循以下规则：

- **可以调整**：子任务执行顺序、子任务完成标准（降级）、子任务预估时间
- **不可以调整**：总目标清单、里程碑时间点、行为边界
- **需要记录**：任何调整都必须在 `decision-log.md` 中记录理由
- **级联调整**：如果降级了某个目标，必须检查是否有其他目标依赖该目标的产出

---

### 4.6 输出规范

#### 产出物分类

| 类别 | 产出物 | 格式 | 质量门禁 |
|------|--------|------|---------|
| 代码 | 源码修改/新增 | Python, 遵循项目风格 | 测试通过 + 无 lint 错误 |
| 测试 | 回归测试 | pytest 框架 | 覆盖主要分支 + 断言明确 |
| 文档 | 设计文档/方案 | Markdown | 含问题分析 + 方案 + 接口 |
| 状态 | 进度/决策/错误日志 | Markdown（按模板） | 实时更新，不遗漏 |
| 报告 | 检查点报告/最终报告 | Markdown（按模板） | 按时生成，内容完整 |

#### 代码变更质量标准

1. **风格一致性**：遵循项目现有代码风格（black, isort, 类型注解）
2. **测试覆盖**：每个新增函数至少有一个对应测试用例
3. **向后兼容**：不破坏已有 API 接口（除非明确说明理由）
4. **文档同步**：修改公共接口时同步更新 docstring
5. **变更最小化**：只修改完成子任务所必需的代码，不做附带重构

#### 最终报告必须包含的章节

```markdown
# 最终状态报告

## 执行摘要
- 运行时长：{X} 小时 {Y} 分钟
- 目标完成度：{N}/7 目标完成，{M} 个降级
- 测试状态：{passed} passed, {failed} failed, {skipped} skipped
- BLOCKED 项：{N} 个
- NEEDS_HUMAN 项：{N} 个

## 各目标完成详情
### G1：KB 知识库临床签核流程
- 完成度：{百分比}%
- 降级情况：{无 / 降级为 Level X}
- 产出文件：{文件列表}
- 测试结果：{通过/失败/N/A}

### G2：...

## 变更文件清单
| 文件 | 操作 | 行数变化 | 关联子任务 |
|------|------|---------|-----------|
| ... | ... | ... | ... |

## 未完成项与建议
### BLOCKED 项
- {子任务ID}：{原因} → 建议：{修复建议}

### NEEDS_HUMAN 项
- {子任务ID}：{原因} → 需要：{具体人工操作}

### 建议的后续工作
1. {高优先级后续工作}
2. {中优先级后续工作}
3. {低优先级后续工作}
```

---

### 4.7 资源与边界约束

#### 工具范围

| 工具类别 | 允许 | 禁止 |
|----------|------|------|
| 文件操作 | Read, Write, Edit, Glob, Grep | 修改 .git/ 目录 |
| 命令执行 | pytest, python, pip install, git status/diff/log | git commit/push/merge, 系统级包安装 |
| 子代理 | 最多 3 个并行 Explore 子代理 | 修改型子代理（仅限只读审计） |
| 网络 | pip 包下载 | 其他外部网络访问 |

#### 时间预算分配原则

时间预算不是平均分配，而是按目标的优先级和预估工时加权分配。但需要预留缓冲：

- **有效工作时间**：总时间的 85%（{total_hours} × 0.85）
- **缓冲时间**：总时间的 15%（用于错误处理、重试、计划调整）
- **检查点时间**：从有效工作时间内扣除（每小时约 5 分钟）

#### 行为边界

行为边界分为三个层次：

**绝对禁止（任何情况下都不可违反）**：
- 不得修改 SOUL.md 人设契约的核心段落
- 不得更改 AGP 2019 共识阈值
- 不得删除或跳过已有回归测试
- 不得在未运行测试的情况下声明任务完成
- 不得执行 git commit/push/merge
- 不得访问真实患者数据

**需要记录（可以做但必须记录理由）**：
- 引入新的第三方依赖
- 修改公共 API 接口
- 降级目标完成标准
- 调整子任务执行顺序

**自由裁量（Agent 自主决定）**：
- 选择实现方案（在有明确偏好时选保守方案）
- 代码风格细节（在遵循项目整体风格的前提下）
- 测试用例的详细设计
- 文档的具体措辞

---

## 5. 检查点模板

> 每个检查点必须按此模板生成报告，写入 `{{state_dir}}/checkpoint-reports/checkpoint-{N}.md`

```markdown
# 检查点报告 #{N}

## 基本信息
- 检查点时间：T+{X}h {Y}m
- 距上次检查点：{N} 小时
- 当前里程碑：{M1/M2/M3/M4}

## 进度摘要

### 计划 vs 实际
| 指标 | 计划 | 实际 | 偏差 |
|------|------|------|------|
| 已完成子任务数 | {N} | {M} | {±X} |
| 已完成目标数 | {N} | {M} | {±X} |
| 已用时间 | {N}h | {M}h | {±X}m |

### 测试状态
- 上次测试运行：T+{X}h
- 结果：{passed} passed, {failed} failed, {skipped} skipped
- 失败测试（如有）：{测试名} — {原因}

### 子任务状态
| 状态 | 数量 | 子任务 ID |
|------|------|-----------|
| 已完成 | {N} | G3-S1, G5-S1, ... |
| 进行中 | {N} | G3-S2 |
| 待执行 | {N} | G1-S1, G1-S2, ... |
| BLOCKED | {N} | G2-S3（原因：...） |
| NEEDS_HUMAN | {N} | G1-S5（原因：...） |

## 偏差分析
{如无偏差：无偏差，按计划执行}
{如有偏差：
- 偏差类型：{进度/质量/资源}
- 偏差原因：{具体原因}
- 影响范围：{受影响的目标和子任务}
- 应对措施：{具体调整}
}

## 决策记录摘要
本检查点周期内做出的关键决策：
- D-{N}：{决策摘要}
- D-{N+1}：{决策摘要}

## 下一小时计划
1. {子任务ID}：{描述}（预估 {X} 分钟）
2. {子任务ID}：{描述}（预估 {X} 分钟）
3. ...

## 风险预警
{当前可预见的风险，如无则写"无"}
- {风险描述} → 预应对方案：{方案}
```

---

## 6. 通用化适配指南

> 本框架以 CGM-Agent 项目为实例，但其结构可适配任何长期工程任务。以下是适配指南。

### 6.1 适配步骤

| 步骤 | 动作 | 输入 | 输出 |
|------|------|------|------|
| 1 | 替换项目现状基线（第 2 章） | 新项目的审计报告/状态文档 | 新项目的现状快照 |
| 2 | 重新定义目标清单 | 新项目的待办事项/路线图 | 5-8 个带优先级的目标 |
| 3 | 调整里程碑时间点 | 总运行时长 + 目标优先级 | 3-4 个里程碑 |
| 4 | 替换行为边界 | 新项目的安全约束/合规要求 | 项目特定的禁止项 |
| 5 | 调整工具范围 | 新项目的技术栈 | 允许/禁止的工具列表 |
| 6 | 更新时间预算 | 各目标的预估工时 | 新的时间分配表 |

### 6.2 通用化变量

主提示词模板中的 `{{}}` 变量及其通用化含义：

| 变量 | 含义 | CGM-Agent 实例值 | 通用化说明 |
|------|------|-----------------|-----------|
| `{{total_hours}}` | 总运行时长 | 8 | 可调整，建议 4-12 小时 |
| `{{state_dir}}` | 状态文件目录 | `.trae/agent-state` | 任何可写的持久化路径 |

### 6.3 目标设定通用化

不同类型任务的目标设定参考：

| 任务类型 | 目标来源 | 成功标准形式 | 典型目标数 |
|----------|---------|-------------|-----------|
| Bug 修复冲刺 | 缺陷追踪系统 | 测试通过 + 复现场景消除 | 5-10 |
| 功能开发 | 产品路线图 | 功能可用 + 验收测试通过 | 3-5 |
| 重构优化 | 技术债清单 | 指标改善（覆盖率/复杂度） | 3-7 |
| 审计修复 | 审计报告 | 发现项关闭 + 回归测试 | 5-15 |
| 迁移升级 | 迁移计划 | 兼容性测试通过 + 旧逻辑移除 | 3-6 |

### 6.4 检查点频率调整

| 运行时长 | 建议检查点频率 | 理由 |
|----------|--------------|------|
| 2-4 小时 | 每 1 小时 | 短运行，频繁校准 |
| 4-8 小时 | 每 1-2 小时 | 中等运行，平衡校准与效率 |
| 8-12 小时 | 每 2 小时 | 长运行，减少检查开销 |
| 12+ 小时 | 每 2-3 小时 | 超长运行，需更强的状态持久化 |

### 6.5 边界约束通用化

不同领域的典型行为边界：

| 领域 | 典型绝对禁止项 |
|------|--------------|
| 医疗/健康 | 修改诊断阈值、删除安全检查、访问真实患者数据 |
| 金融 | 修改交易逻辑、绕过审计日志、访问生产交易数据 |
| 安全系统 | 修改认证逻辑、降低加密强度、暴露密钥 |
| 通用 | 删除测试、跳过 CI、修改依赖版本锁定文件 |

### 6.6 降级策略通用化

不同任务类型的降级阶梯参考：

| 任务类型 | Level 0 | Level 1 | Level 2 | Level 3 |
|----------|---------|---------|---------|---------|
| Bug 修复 | 修复 + 测试 | 修复 + 部分测试 | 修复方案 + 临时补丁 | 问题分析 + 方案 |
| 功能开发 | 完整功能 + 测试 | 核心功能 + 测试 | 接口定义 + Mock | 设计文档 |
| 重构 | 完整重构 + 测试 | 部分重构 + 测试 | 重构方案 + 示例 | 重构计划 |
| 文档 | 完整文档 | 核心章节 | 大纲 + 关键章节 | 大纲 |

---

*本框架为 CGM-Agent 项目专属实例化版本。框架的七维度结构（目标设定、任务分解、状态管理、自主决策、检查点评估、输出规范、资源约束）适用于任何需要 AI Agent 超长时间自主运行的工程任务场景。*
