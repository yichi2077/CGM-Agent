# 已知限制（Known Issues & Limitations）

> 本文件随 v0.1.0 发布，如实记录当前版本的能力边界。条目按重要程度排序。
> 状态来源：docs/BACKLOG.md（单一事实源）与 2026-07-07 发布前审计。

## 1. ⚠️ 知识库内容未经临床核验（最重要）

权威医学知识库（`authoritative_kb.json`）当前 **580 张卡片全部为
`verified=false`**（574 张为机器抽取的 `auto` 层，6 张 `curated` 层）。
临床签核工具链（`kb.approve` / `kb-approve`：支持 auto/curated 层、强制 reviewer 署名、幂等）
已就绪，但**尚无临床审核者完成任何一张卡的签核**。

这意味着：报告中"医学参考"段引用的数字来自机器从医学 PDF 中抽取、
未经人工复核的内容。引用硬门保证生成叙事中的数字在卡片中有出处，
但**不保证卡片本身抽取无误**。请把医学参考段视为"文献线索"而非医嘱，
任何医疗决定请咨询医生。

**主题覆盖现状**：低血糖处置（15-15 法则、分级、胰高血糖素急救）、CGM 数据质量、
TIR/TBR/TAR/GMI/CV、酮体检测（≥250 mg/dL / 生病）、成人/妊娠/老年/儿童血糖目标等核心安全
主题已有卡片覆盖，并通过 73 条检索回归查询验证可被口语化提问召回。**尚缺独立的"运动性低
血糖管理"与"病假日规则（不停药/补液/加强监测）"原子卡**（当前仅有相邻卡片的部分覆盖）。
这些缺口应通过既有的自动摄取管线（`kb-ingest-batch --engine hermes` 覆盖对应指南页）补齐，
再经临床审核者签核，而非人工手写——避免在无权威来源支撑下臆造医学结论。

## 2. 推送通道成熟度

- **Webhook**：已实现并受测（HTTPS-only、禁重定向、PHI 白名单过滤）。
- **Email（SMTP）**：代码已实现（配置 `CGM_SMTP_HOST`/`CGM_SMTP_TO_ADDRESS`
  后即真实发信，正文同样经 PHI 白名单过滤），但未经生产环境长期验证，
  定位为可选通道；未配置时投递记录状态为 `queued`。
- **月报**：推送节奏支持 monthly tier，但月报叙事模板尚未实现。
- **PDF 导出**：未实现，报告输出为 Markdown。

## 3. 数据源支持范围

硬件已确认为**微泰 AiDEX X**（见 ADR-0002、D063、D064）。厂商 API（微泰
Open API、Dexcom v3）无权限且已于 2026-07-14 整体移除。当前可用的数据入口：

- 安卓桥（`bridge-poll`/`cgm_bridge_poll.py`）：默认 **xDrip 伴侣模式**
  （官方 App 保留 BLE/报警,xDrip 抓通知),局域网 HTTP → 真实数据唯一路径,
  真机验证待完成;Juggluco 直连为数据完整性备选,Nightscout 为跨网中继;
- xDrip / Juggluco / Nightscout HTTP 手动采集（`source-poll`,诊断用）;
- CSV 导入（`import-cgm`）,支持 mg/dL 与 mmol/L 常见写法。

**已知限制（D064）**:伴侣模式为通知捕获,有损——社区报告每小时丢 2-4 个
读数（覆盖率 ~95%）。本桥定位为**分析级数据源,非报警级**;实时低/高血糖
报警仍由官方 App 负责,本系统不承担报警职责（宪章 III）。若数据缺口影响
分析质量,可切 Juggluco 直连（代价:官方 App 报警停用）。设置
`CGM_WEBHOOK_URL` 后,新鲜度看门狗会在断流/恢复边界各发一条 PHI-free 告警。

## 4. 单用户假设

本项目按**个人单用户部署**设计。默认身份为 `demo-user`（建议设置
`CGM_AGENT_USER_ID`）。多用户共库的权限隔离未设计。红区恢复复查窗口
（2 小时）已持久化到 SQLite（TD2 修正），进程重启后恢复窗口保留。

## 5. 数据权利与备份

- 所有 PHI 字段经 Fernet 应用层加密存储于本地 SQLite；**加密密钥
  `storage.key` 丢失则历史数据不可恢复**，请与数据库文件一同备份。
- 尚无独立的"一键导出/删除全部数据"命令；本地部署下删除数据库文件
  目录（DB + key）即等效于删除全部数据。

## 6. 检索能力

个人记忆检索默认 BM25 词法检索；语义 dense 向量检索为可选依赖
（`pip install -e ".[semantic]"`），在个人数据规模下 BM25 已足够。
医学 KB 轨恒为 BM25（设计约束，防止未核验卡稀释）。

## 7. 测试基线说明

README 中的测试基线为**离线确定性套件**。真实 Hermes + LLM 的端到端
测试需显式开启：`CGM_RUN_HERMES_E2E=1`（会产生真实 API 调用与费用）。
